version: 2

models:

  - name: amplitude__event_enhanced
    description: Each record
    columns:
      - name: event_id
        description: id
        tests:
          - unique
          - not_null